name: Lighthouse CI
on: [pull_request, pull_request_target]

jobs:
  Lighthouse:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Extract branch name
        shell: bash
        run: echo "##[set-output name=branch;]$(echo ${GITHUB_REF#refs/heads/})"
        id: extract_branch
      - uses: actions/checkout@master
      - name: Lighthouse Audit Branch
        env:
          LHCI_COMMENT_TOKEN: ${{ secrets.LHCI_COMMENT_TOKEN }}
          LHCI_GITHUB_ACCESS_TOKEN: ${{ secrets.LHCI_GITHUB_ACCESS_TOKEN }}
        uses: foo-software/lighthouse-check-action@master
        with:
          outputDirectory: .
          device: "all"
          urls: "https://receta-git-${{ github.base_ref }}-shorttryan.vercel.app, https://receta-git-${{ github.base_ref_target }}-shorttryan.vercel.app"
          extraHeaders: "{\"Cookie\":\"auth=${{ secrets.LHCI_GITHUB_ACCESS_TOKEN }}\"}"
          prCommentEnabled: true
          prCommentSaveOld: true
          gitHubAccessToken: ${{ secrets.LHCI_COMMENT_TOKEN }}
          gitAuthor: ${{ github.actor }}
          gitBranch: ${{ github.ref }}
      - name: Upload artifacts
        uses: actions/upload-artifact@master
        with:
          name: Lighthouse reports
          path: .
